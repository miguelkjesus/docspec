# Load nvm if available (works across different setups)
export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" --no-use
[ -s ".nvmrc" ] && nvm use --silent || true

npx lint-staged
